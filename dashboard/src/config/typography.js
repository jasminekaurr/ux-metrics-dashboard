/**
 * typography.js — Shared font stacks for dashboard UI.
 *
 * Prefer CSS `var(--font-mono)` / `var(--font-sans)` in stylesheets.
 * Use these exports only where Chart.js or inline style APIs need a string.
 */

export const FONT_MONO = '"Gt America Mono", ui-monospace, Consolas, monospace'
export const FONT_SANS = '"Alliance No. 2", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif'
